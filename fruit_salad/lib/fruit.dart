import 'package:flutter/material.dart';

class Fruit {
  String name;
  Color color;
  final double price;
  int clickCount;
  final String pic;

  Fruit(this.name, this.color, this.price, this.clickCount, this.pic);
}